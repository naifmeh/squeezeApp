<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="titleLabels">
        <item name="android:textColor">@color/mainTextColorTitles</item>
        <item name="android:textSize">@dimen/titleMainLabel</item>
        <item name="android:fontFamily">sans-serif-black</item>
    </style>

    <style name="recyclerViewLabels">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/titleMainLabel</item>
        <item name="android:fontFamily">sans-serif-medium</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="titleTextColor">@color/floralWhite</item>
        <item name="android:textColorSecondary">@color/floralWhite</item>

    </style>

    <style name="TransparentToolbar" parent="Base.ThemeOverlay.AppCompat.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <!-- Support library compatibility -->
        <item name="windowActionBarOverlay">true</item>
    </style>
</resources>
